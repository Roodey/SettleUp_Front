{{!--{{outlet}}--}}
<!--Left side column on page-->
<!--<div class="row">
  <div class="col-md-8 col-sm-12 ideaCatalog">
{{#each model as |idea|}}
<div class=" col-sm-4 col-md-4">
  <div class="panel panel-default">
    <div class="panel-heading">{{idea.title}}</div>
    <div class="panel-body">{{idea.description}}</div>
    <div class="panel-footer">
      <div class="float-md-right">
      {{#link-to "idea.index" idea.id}}
        View
      {{/link-to}}
    </div>
    </div>
  </div>
</div>
{{/each}}
</div>-->

<!--Right side column of the page-->
<!--
<div class='col-md-4 hidden-sm updateCatalog'>
  <h3 class='updateTitle'>Project Updates!</h3>
</div>
</div>
-->
{{outlet}}
